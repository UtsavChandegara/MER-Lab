"""Registry for Unimodal Encoders and Projections (MER-RULE-015, MER-RULE-065)."""

from src.foundation.registry import Registry

encoder_registry = Registry("encoder")
projection_registry = Registry("projection")
